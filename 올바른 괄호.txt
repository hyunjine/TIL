from collections import deque
def solution(s):
    Q = deque(list(s))
    stack = [Q.popleft()]
    while Q:
        v = Q.popleft()
        if v == ("("):
            stack.append(v)
        else:
            if not stack:
                return False
            else:
                stack.pop()
    return False if stack else True