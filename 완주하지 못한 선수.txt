def solution(participant, completion):
    dic = {}
    for p in participant:
        if dic.get(p) == None:
            dic[p] = 1
        else:
            dic[p] += 1
    for c in completion:
        dic[c] -= 1
        if dic[c] == 0: 
            del dic[c]
    return list(dic.keys())[0]